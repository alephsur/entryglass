# Nansen Adapter

Reserved for the HTTP client, provider DTOs, response normalization, evidence
metadata, and usage accounting. No client is implemented. Follow
`docs/NANSEN_INTEGRATION.md` at the repository root before adding requests.
The normal test suite must remain offline and must not consume provider credits.
