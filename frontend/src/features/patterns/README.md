# Personal Precedents

Implemented in the main local journey. The user explicitly reveals a same-wallet
baseline and four versioned Smart Trader flow groups. Each group shows sample and
outcome counts plus its matching entries. The view is descriptive only and exposes
missing context and small-sample limitations.
