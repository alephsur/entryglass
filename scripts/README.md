# Development Scripts

`setup.sh` installs local dependencies and creates a root `.env` only when one does
not already exist. It checks tool availability and the Node version first. It does
not create a Nansen account, run analyses, send paid requests, publish anything, or
initialize a remote repository. Run it with Bash from the extracted project.
